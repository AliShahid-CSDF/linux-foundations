Commit: "Add Day 1 notes: Linux filesystem hierarchy and basic navigation"

# Week 1 – Linux Filesystem & Navigation
Practiced on Kali Linux (XFCE).
- Mapped the FHS tree: /, /home, /etc, /var/log, /usr/bin, /tmp
- Commands: pwd, ls -la, cd (absolute vs relative), mkdir -p, touch, cat
- Explored /etc/os-release and /var/log as forensic starting points
- Next: users, permissions, ownership
